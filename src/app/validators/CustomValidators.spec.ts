import { CustomValidator } from './CustomValidators';
import { FormGroup, Validators, FormControl } from '@angular/forms';

describe('Custom Validators', () => {


/*
  it('12 es un número válido', () => {
    const form = new FormGroup({
      campo: new FormControl(12, CustomValidator.numeric)
    });

    const spy = spyOn(CustomValidator, 'numeric');
    const result = spy.and.callFake(msg => form.get('campo'));
    expect(result).toEqual(null);
  });
 */
});
